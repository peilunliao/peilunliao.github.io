1.
(A)
#include <stdio.h>
#include <stdlib.h>


int main(){
    
    char input[256];
    int i = 0 ,reverse_i;
    
    printf("input: ");
    gets(input);
    
    while (input[i] != '\0')
        i++;
    for(reverse_i = i-1; reverse_i >=0 ; reverse_i--)
        printf("%c",input[reverse_i]);
    
    return 0;
}
(B)
#include <stdio.h>
#include <stdlib.h>


int main(){
    
    char input[512];
    
    int i = 0, j, reverse_i;
    
    printf("input: ");
    
    scanf("%[^\n]", input);
    
    while (input[i] != '\0'){
        
        j = i;
        
        while(input[i] != 32 )
            i++;
    
        for(reverse_i = i-1; reverse_i >=j ; reverse_i--)
            printf("%c",input[reverse_i]);
        
        i++; /*jump to next word*/
    }
    
    return 0;
}
2.
#include <stdio.h>

int main(){
    int num;
    int i, n = 0;
    
    printf("input: ");
    scanf("%d",&num);
    
    for( i = 1; i <= num; i++)
    {
        if((i % 3) ==0 && (i % 15) != 0)
            continue;
        else if((i % 5) ==0 && (i % 15) != 0)
            continue;
        else
            n++;
    }
    
    printf("output: %d", n);

    return 0;
}

3.
選擇混合的袋子，
拿出的是鉛筆，表示鉛筆的袋子為原子筆，而裝原子筆的袋子是混合的。
若拿出的是原子筆，表示鉛筆的袋子是混合的，原子筆的袋子是鉛筆。

4.
三人各出 270 元 270元 × 3人 + 服務生暗扛的 60 元
此段話有誤，服務生所暗扛的金額是在此三人實際付出金額裡去拿的，
因此不能再加上60元,因此870元數字本身是無意義的，
沒有所謂的消失的30元，只是敘述上的問題。
